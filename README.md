# homebrew-radial

Homebrew tap for the [Radial](https://github.com/BrainGridAI/radial) CLI.

```bash
brew install BrainGridAI/radial/radial
```

`Formula/radial.rb` is generated and bumped automatically by Radial's
`cli-release` workflow on each `cli-v*` tag — **do not edit it by hand.**
